[credential]
    helper = osxkeychain
[user]
    name = Daniel Rubens
    email = danielrubensdaniel@gmail.com
[push]
    followTags = true
[core]
    editor = code --wait
[alias]
    s = !git status
    c = !git add --all && git commit -m
    l = !git log --pretty=format:'%C(blue)%h%C(red)%d %C(white)%s - %C(cyan)%cn, %C(green)%cr'
